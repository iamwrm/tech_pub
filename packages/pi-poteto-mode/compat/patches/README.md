# Compatibility patches

Prefer a complete file or directory under `../overrides/<skill>/` when an
upstream skill needs semantic adaptation. This directory is reserved for small,
reviewable patches that should fail when their upstream context drifts.
